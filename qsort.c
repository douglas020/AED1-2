


int main (){

    if (Node == NULL){
        return NULL;
    }

    for (int i =0 i<k;i++){
        while (Node[i] != NULL){
            array = realloc(size * sizeof(int));
            array[i] = Node->val;
            size++;
            index++;
        }
    
    }

    qsort[array];

    for (int i=0;i<size;i++){
        insere(retorna,arrau[i]);
    }

    return retorna;
}